# One window per account

Hover an account in the sidebar and click the new-window icon. That window is a dedicated session:
its own folder, its own key, its own title-bar color, and a sidebar focused on just that client.
Run parallel windows for parallel clients without mixing anything.
